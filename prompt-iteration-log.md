# Prompt 迭代日志

> 记录每次 Prompt 修改的前后对比。每个版本包含：触发原因 → 修改了什么 → 修改前后对比 → 副作用评估。
>
> Git 历史（`git log -- prompt-iteration-log.md`）提供时间戳背书。

---

## v7 — 2026-08-11（Prompt 瘦身 + 性价比优化）

### 触发原因

v6 全部功能达标，但 LLM 节点在 5 只 ETF 场景下消耗 6000+ tokens / 43s+。通过 Dify 日志分析确认 tokens 与耗时严格正相关（每 token ~7ms），唯一降本路径是压缩 Prompt 体积。目标：在不牺牲 P0/P1 质量的前提下，将 5 只 ETF 场景的 tokens 控制在 4000 以内、耗时控制在 25s 以内。

### 修改了什么

**第一轮：System + User Prompt 瘦身（commit `22030f4`）**

- System Prompt：行为准则 6 条从带示例的冗长版压缩为简短核心约束；术语指南删掉"首次出现"等执行细节保留定义；输出格式从多行分点压缩为紧凑一行式描述；分析流程段删除（后恢复）
- User Prompt：删除与 System Prompt 重复的"严禁"类指令；日期指令简化为 `当前日期：{{ current_date }}`；warning 提示从 3 行压缩为 1 行
- 效果：System Prompt ~1380 字 → ~680 字（-49%），User Prompt ~380 字 → ~180 字（-53%），总计省 ~500 input token

**第二轮：修复 3 处退化（commit `19ce51c`）**

C1 测试发现 Prompt 瘦身后 P1 有用性退化：
1. 术语指南加了执行指令：`报告中出现以下术语时，附带一句白话解释：`
2. 维度对比总结加了深度要求：`差异最大的 2-3 个维度，说明差异意味着什么。`
3. 注意事项加了质量约束：`基于本次数据的风险点。`

**第三轮：恢复轻 CoT（commit `e98c8c9`）**

C1 重测后发现模型把"最新价"当成核心差异维度（同一指数下 ETF 价格无比较意义），证实完全删除 CoT 导致模型失去了区分"有意义维度"和"无意义数值"的判断力。恢复 `## 分析流程` 段 + 一句引导：`生成报告前，快速判断本轮对比最显著的差异维度，据此决定侧重点和风险提示方向。`

### 全量测试总结（跟踪误差修复前）

| Case | 场景 | tokens | 耗时 | P0 准确性 | P1 有用性 | P2 一致性 |
|------|------|--------|------|-----------|-----------|-----------|
| C1 | 2 只宽基（510300+510310） | 2227 | 16.1s | ✓ | ✓ 维度消解/核心矛盾/术语白话 | ✓ |
| C2 | 2 只跨行业（159995+512690） | 4525 | 28.5s | ✓ | ✓ 归因分离/跟踪误差标准/注意事项 | ✓ |
| C3 | 单 ETF 深度解读（510300） | 3055 | 21.7s | ✓ | ✓ 三要素纪律/无外部知识/无竞品 | ✓ |
| C4 | 5 只边界 | 3846 | 21.9s | ⚠️ 3只显示"暂无数据" | ✓ | ✓ |

---
### v7.1 — 2026-08-12（跟踪误差抓取重试）❌ 已回滚

#### 触发原因

C4 测试（5只ETF）中，159915/512880/588000 三只 ETF 跟踪误差显示"暂无数据"。根因：5 只 ETF 并发时，`basic.py` 的 `max_workers=5` × `fetch_fund_detail()` 的 `max_workers=3` = 最多 15 个并发请求打向东方财富，触发限流。

#### 尝试方案（已回滚）

`_shared.py` 两处增加首次失败后延迟 1.5s 重试逻辑。

#### 结果：失败

C4 重测后 5 只全部"暂无数据"（原 2 只可用）。重试加倍了请求量，加剧限流。

---
### v7.2 — 2026-08-12（降低并发度）❌ 已回滚

#### 触发原因
v7.1 的重试方案恶化问题，转向从源头减少并发请求数。

#### 修改了什么
`basic.py` `max_workers`: 5→3, `fetch_performances` `max_workers`: 5→3

#### 结果：失败 + 新增回归
C4 重测后跟踪误差依旧全空，且实时行情（最新价/涨跌幅/成交额）也全变成"暂无数据"——由独立的 Sina `hq.sinajs.cn` Vercel 不可用导致。

---
### v7.3 — 2026-08-12（跟踪误差换 tsdata 页面 + Sina HTTP）

#### 触发原因
1. 跟踪误差：`fund.eastmoney.com/{code}.html` 的年化跟踪误差是 JS 动态渲染，`urllib` 抓不到
2. 实时行情：Sina `hq.sinajs.cn` 在 Vercel 上返回空（疑似 HTTPS/TLS 兼容问题）

#### 修改了什么

| 文件 | 改动 |
|------|------|
| `_shared.py` `fetch_fund_detail()` | 主页面 → `fundf10.eastmoney.com/tsdata_{code}.html`（静态 HTML 含年化跟踪误差） |
| `_shared.py` `_fetch_single_performance()` | 同上 |
| `_shared.py` `fetch_snapshots()` | `https://hq.sinajs.cn` → `http://hq.sinajs.cn`（避开 Vercel TLS 兼容问题） |

#### 代价 / 副作用
- tsdata 页面对部分新 ETF 可能不存在（fallback 为 null 即"暂无数据"），可接受
- Sina HTTP 明文传输，数据非敏感无影响
- 不涉及 Prompt 变化。

#### 测试结果

| Case | 跟踪误差 | 实时行情 | 耗时 | tokens |
|------|---------|---------|------|--------|
| C4 (5只) | 5/5 全部有数据 | 全部正常 | 26.6s | 4112 |

vs v7 baseline C4: 3 只"暂无数据" → 全部修复，耗时仅增加约 4.7s（tsdata 额外请求），tokens 略增（跟踪误差维度进入分析）。

#### 部署状态
- commit: v7.3，已部署到 Vercel
- v7.1 / v7.2 已回滚

---
### v7.4 — 2026-08-12（单 ETF 改为表格输出）

#### 触发原因
C3 单 ETF 模式当前用段落文字描述，信息密度低、不直观。用户要求改为表格形式。

#### 修改了什么
`workflow-dsl.yml` System Prompt 单 ETF 输出格式：
- 原: `基本画像 → 指标解读 → 潜在关注点` 三段式文字
- 新: `基本画像`(表格) → `费率与跟踪`(表格) → `收益表现`(表格) → `潜在关注点`(文字)

表格列明具体数据项，无需模型自行组织措辞。

#### 代价 / 副作用
- 需重新导入 DSL 到 Dify
- 不涉及 API/Prompt 逻辑变更，不影响 C1/C2/C4

#### 部署状态
- 代码无需部署（仅 DSL 变更，Vercel 不受影响）
- commit: 已跑通 C3，v7 最终版

#### 测试结果
C3 表格输出清晰：基本画像/费率与跟踪/收益表现 三表格 + 潜在关注点。用户确认满意。

---
## v7 最终全量汇总

| Case | 场景 | tokens | 耗时 | 数据完整性 | 
|------|------|--------|------|-----------|
| C1 | 2 只宽基 | 1800 | 12.8s | 全部完整 |
| C2 | 2 只跨行业 | 3847 | 23.3s | 全部完整 |
| C3 | 单 ETF 表格 | 1292 | 10.0s | 全部完整 |
| C4 | 5 只边界 | 4112 | 26.6s | 全部完整 |
| **平均** | | **2763** | **18.2s** | |

vs v6: tokens -42%, 耗时 -43%, 质量持平, 数据完整性提升（跟踪误差 tsdata 静态页面 + 实时行情 Sina HTTP）

---
### v7 vs v6 基准

| 指标 | v6 C4 | v7 C4 | 变化 |
|------|-------|-------|------|
| tokens | 6037 | 3678 | **-39%** |
| 耗时 | 43.2s | 20.7s | **-52%** |
| 平均 tokens | ~5800 | ~3371 | **-42%** |
| 平均耗时 | ~38s | ~21.8s | **-43%** |
| 质量 | 全部达标 | 全部达标 | **持平** |

### 代价 / 副作用

- 行为准则从详细示例压缩为简短约束，极端场景下（如模型温度波动）约束力可能弱于 v6 的详细版。当前 v7 测试未触发此风险，但若未来出现新模型版本或低质量采样，可能需要恢复详细示例。
- 分析流程保留了一句 CoT，已在 C1 中验证能阻止"最新价"等无意义维度进入核心差异。若 C4 重测时发现其他无意义维度混入，优先加输出格式约束（方案 A）而非扩展 CoT。
- 单 ETF 模式的"缺乏横向对照"坦诚说明跑通了，但若后续需要更丰富的单 ETF 解读，可以加模板但不应跨过行数/结构限制。

### 部署状态

- 全部改动仅涉及 System Prompt + User Prompt（DSL），不动 API 代码
- 最终 commit：`e98c8c9`，需重新导入 DSL
- C1~C4 全部通过，C5（无效代码）确认跳过

---

## v6 — 2026-08-11（行为准则补齐：禁推断/禁预测/禁补知识）

### 触发原因

v5 C1~C5 全量测试后遗留 7 个 P1/P2 问题，共性是 **LLM 在数据不足时主动补内容**——C2 补推断/预测，C3 补外部知识/竞品/章节。

### 修改了什么

全部只改 System Prompt（DSL），不动 API 代码。

**改动 A：行为准则第 1 条扩展**（修复 #4 预测性表述）

```diff
- 1. 禁止提供投资建议和产品层优劣判断
+ 1. 禁止提供投资建议、预测性表述和产品层优劣判断。不说"建议买入"、"推荐持有"，
+    不说"如果市场延续…则可能…"等条件预测，不说"这只更好"、"明显更优"等结论性表述
```

**改动 B：行为准则新增第 6 条**（修复 #2 特征定性、#3 形态推断、#5 外部知识、#6 竞品参照）

```
6. 禁止脱离数据做推断或补充。只陈述数据中已有的信息，不推断数据背后未显示的规律
   （如"防御性品种""横盘+急跌"等形态描述），不引入数据中未提及的外部知识、竞品比较或行业参照
```

**改动 C：术语指南成立日期**（修复 #1 时长算错）

```diff
- - 管理费率、托管费率、成立日期出现时也需简短白话解释
+ - 成立日期：直接引用数据中的日期即可，无需计算运作时长
```

**改动 D：单 ETF 输出格式加结构纪律**（修复 #7 结构漂移）

```diff
- 生成「深度解读报告」，包含：
+ 严格按以下三要素输出，不可新增段落、不可拆分独立章节：
+ - …（必须基于给定数据，不可引入数据外的竞品或行业参照）
```

**改动 E：CoT 分析流程压缩**（缓解深度思考耗时过长）

```diff
- 生成报告前，先在心里完成以下分析：
- 1. 识别核心矛盾：这几只 ETF 之间"最值得关注的分化点"是什么？
-    - 可能是费率差距、规模差距、行业/指数的本质差异、或业绩分化
- 2. 根据核心矛盾，决定本次报告的侧重点和篇幅分配
- 3. 确定注意事项中需要特别提示的风险点
+ 生成报告前，快速判断本轮对比最显著的差异维度，据此决定侧重点和风险提示方向。
```

触发原因：DeepSeek V4 Flash 在 Dify 中偶尔触发深度 CoT（思考 1-2 分钟），而 v5+ 表格已预渲染、数据已分类，三个逐层递进的问题大部分是空转。压缩为一句可保留引导但大幅减少思考 token。

### 修复对照

| # | 问题 | 修复 |
|---|------|------|
| 1 | 成立日期时长算错 | 改动 C |
| 2 | 无数据支撑的特征定性 | 改动 B |
| 3 | 有限数据强行推形态 | 改动 B |
| 4 | 预测性表述 | 改动 A |
| 5 | 注入无关领域知识 | 改动 B |
| 6 | 臆造竞品参照 | 改动 B + D |
| 7 | 单 ETF 结构漂移 | 改动 D |
| — | CoT 过度深度思考耗时过长 | 改动 E |

### CoT 深度思考优化（v6 第二轮，commit `3f8fc72`）

**触发原因**：v6 C4 测试后用户反馈 DeepSeek 仍出现超过 60 秒的深度思考。通过 Dify 后台查看 CoT 原始过程（超 6000 字），发现三个触发源：

1. **"首次出现"状态追踪**：术语指南写"报告中首次出现以下术语时，必须用一句白话解释" → 模型在脑子里追踪每个术语的"第一次出现位置"，做完整报告的状态管理
2. **行数计数**：逐只解读格式写"不超过 5 行" → 模型逐行数字数，反复调整措辞
3. **费率算术**：术语指南写"费率相差 0.1% 以上时，应计算 5 年累计成本差异" → 模型触发条件判+算术推理链

**改动 F：术语指南去状态约束**（删除"首次出现"）

```diff
- 报告中首次出现以下术语时，必须用一句白话解释：
+ 对以下术语给出白话解释：
```

**改动 G：术语指南费率去算术指令**（计算移到代码，同时修复 C4 的"直接引用即可"指令泄露残留）

```diff
- 综合费率：管理费 + 托管费…两只 ETF 费率相差 0.1% 以上时，应计算 5 年累计成本差异
- 成立日期：直接引用数据中的日期即可，无需计算运作时长
+ 综合费率：管理费 + 托管费（两者每年从净值中扣除），是持有 ETF 的核心成本
+ 成立日期：基金正式开始运作的日期
```

**改动 H：逐只解读去行数约束**

```diff
- 每只 ETF 一段话（不超过 5 行），突出该 ETF 在本轮对比中最显著的特点
+ 每只 ETF 一段简洁描述，突出该 ETF 在本轮对比中最显著的特点
```

**改动 I：聚合 Code 节点预计算费率差异**（⚠️ 需重新导入 DSL）

在表格下方附加费率差异摘要（LLM 直接引用，不再自行计算）：
```python
# 5.5. 预计算费率差异
if diff >= 0.1:
    lines.append(f"> 📊 费率差异：最高综合费率较最低高出 {diff:.1f} 个百分点，"
                 f"5 年累计持有成本多约 {diff_5yr:.1f} 个百分点（未计复利）。")
```

### Vercel 代理超时修复（v6 第三轮，commit `f4b6a1b`，仅改 `api/compare.js`）

**触发原因**：CoT 优化后 Dify UI 内测试已很快，但网页端仍需 1-2 分钟。根因在 `compare.js` 使用了 `response_mode: 'blocking'`——Dify 将所有节点+LLM 结果打包完才一次性返回，Vercel Hobby 60s 硬限制随时可能触发。

**修复**：`blocking` → `streaming`，Vercel 函数逐块读取 Dify SSE 事件流，提取 `workflow_finished` 事件中的报告内容，最终仍以 JSON 格式返回前端（前端零改动）。streaming 模式下 I/O 持续活跃，避免 Vercel 判定函数"无响应"而提前杀进程。

### v6 全量测试总结

| Case | 场景 | 结果 | 关键验证 |
|------|------|------|----------|
| C1 | 2 只宽基（510300+510310） | ✓ | 7 问题未复发、CoT 压缩后维度总结未退化 |
| C2 | 2 只跨行业（159995+512690） | ✓ | 跟踪误差按行业 ETF 标准、无预测/推断/外部知识 |
| C3 | 单 ETF 深度解读（510300） | ✓ | 严格三要素、无竞品参照、无结构漂移 |
| C4 | 5 只边界 | ✓ | CoT 耗时从 60s+ 骤降、费率预计算正常引用 |
| C5 | 无效代码 | ⏭️ 用户确认可跳过 |

### 代价 / 副作用

- 行为准则从 5 条扩展到 6 条，增加了 Prompt 长度。但 6 条均可对应具体 bad case（非泛化规则），不算过度约束。
- "禁预测"可能抑制 CoT 在注意事项中的合理风险提示。观察：如果 C2 重测后"注意事项"变成纯数据复述而无判断力，需调整措辞放宽"预测"的边界（区分"无根据推断"和"基于数据的风险识别"）。
- CoT 压缩后模型可能抓不住多 ETF 的核心矛盾，导致「维度对比总结」退化为"平均分列"。观察：C4 重测时若出现此退化，可恢复 CoT 但加一句"以上分析过程限制在 100 字以内"。

### 部署状态

- 全部改动已提交推送：System Prompt（commit `3f8fc72`，需重新导入 DSL）、Vercel 代理（commit `f4b6a1b`，自动部署）
- C1~C4 全部通过，C5 跳过
- 网页端响应时间显著改善

---

## v5 — 2026-08-11（表格稳定性 + 日期动态化 + 数据源可靠性）

### 触发原因

v4 C1~C5 测试后遗留 4 个问题：

| # | 问题 | 严重度 | 现象 |
|---|------|--------|------|
| P1-3 | 对比总览表格列丢失 + 转置 | **P1** | C1 丢 1 列 → C2 丢 2 列 → C4 丢 7 列 + 表格转置（维度作行→ETF 作行），三次复现、逐次恶化 |
| P2 | 日期上下文硬编码 | **P2** | User Prompt 写死 `2026 年 08 月`，无法自动翻月 |
| 4A | 注意事项"历史业绩"发散 | **中** | C4 出现"可能已包含市场情绪因素，不应对同类回报进行线性外推"，超出"历史业绩不代表未来表现"应有范畴 |
| 4B | 跟踪指数关键词匹配漏洞 | **中** | "证券ETF国泰"不匹配关键词"证券公司"、"酒ETF鹏华"不匹配"白酒/中证白酒"，导致两个 ETF 的跟踪指数+跟踪误差均为"暂无数据" |

### 方法论讨论

**关键决策 1：表格构建——Prompt 约束 vs 代码生成**

v4 用 System Prompt "表格列不可变"试过格式纪律——无效。问题本质是：当 LLM 有 17 个字段的自由选择权，它倾向于"智能选列"以优化可读性。两种解法：

| | 方案 A：Prompt 约束 | 方案 B：代码预渲染 |
|------|-------------------|-------------------|
| 做法 | Prompt 列出全部 17 个必选字段 | 聚合 Code 节点把表格渲染成完整 Markdown 字符串，LLM 一字不改照抄 |
| 消除列丢失 | 概率性（模型仍可能"忘记"） | 确定性（Python 循环保证全量） |
| 表格形态 | 可能转置 | 100% 确定（维度作行 × ETF 作列） |

选择 B。理由：表格构建是确定性操作（固定字段 × N 只 ETF → 拼字符串），不应该交给概率模型。且聚合节点已有全部格式化逻辑，多拼一个表头行不增加复杂度。

**关键决策 2：跟踪标的数据源——关键词匹配 vs 页面直接抓取**

v1~v4 用 `infer_tracking_index()` 做 ETF 名称关键词匹配来确定跟踪指数，如"酒ETF鹏华"含"酒"→猜"中证白酒指数"。但实际 512690 跟踪的是**中证酒指数**（399987），不是中证白酒指数（399997）——关键词匹配在这里犯了两次错误：

1. 证券ETF国泰（512880）：关键词"证券公司"不匹配"证券ETF" → 漏掉
2. 酒ETF鹏华（512690）：匹配也不漏但猜错（"酒"→白酒而非酒）

根本问题不在关键词列表不够全，而在于**关键词匹配是"推断"而页面是"权威"**。东方财富基金概况页（`fundf10.eastmoney.com/jbgk_{code}.html`）已经明确写着 `跟踪标的：中证酒指数`——既然我们已经在下载这个页面提取成立日期和费率，再加一行正则提取跟踪标的，零额外 HTTP 调用。

**关键决策 3：日期注入——独立节点 vs 聚合节点嵌入**

Dify Workflow 没有内置"当前时间"系统变量可直接用于 Jinja2 模板。两种可行方式：

| | Dify 独立 Code 节点 | 聚合节点嵌入 |
|------|--------------------|------------|
| 节点数 | +1（新增时间节点） | 不变 |
| 延迟 | +~50ms | 0 |
| Dify Cloud 计费 | 每次多一次节点调用 | 不增加 |

选择聚合节点嵌入。理由：聚合节点的职责是"给 LLM 准备所有上下文数据"，当前日期就是一种上下文数据。且 Dify Cloud 按节点调用次数计费，加独立节点意味着每次对比多一次计费。

### 修改了什么

#### 改动 A：聚合节点预渲染 Markdown 表格（P1-3 修复）

**改动前**：聚合节点输出 key-value 格式 JSON，LLM 自行构建表格：
```
### ETF1名称(代码)
- 跟踪指数: xxx
- 基金公司: xxx
...
```

**改动后**：聚合节点直接输出完整 Markdown 表格字符串：
```
## ETF 对比数据表格

| 对比维度 | ETF1(代码) | ETF2(代码) |
| :--- | :--- | :--- |
| 跟踪指数 | xxx | xxx |
| 基金公司 | xxx | xxx |
...（17 行完整维度）
```

LLM 的 System Prompt 对应改为："直接使用 <etf_raw_data> 中已提供的 ETF 对比数据表格，一字不改地放入此章节。"

**代价**：LLM 不再参与表格构建 → 表格永远 17 维 × N 列 → 但后续新增维度需同时改聚合节点的 `FIELD_ORDER` 列表 + INDEX_CODE_MAP（已集中在聚合节点）。

#### 改动 B：聚合节点加 `current_date` 字段（P2 修复）

```python
from datetime import datetime
# ...
return {
    "comparison_data": "\n".join(table_lines),
    "current_date": datetime.now().strftime("%Y 年 %m 月"),
    ...
}
```

聚合节点 outputs 新增 `current_date` 字段，LLM 节点 jinja2_variables 注册为模板变量。

#### 改动 C：User Prompt 日期动态化

```diff
- 当前实际日期为 2026 年 08 月。
+ 当前实际日期为 {{ current_date }}。
```

#### 改动 D：System Prompt 对比总览改为引用预构建表格

```diff
- ### 对比总览
- 用 Markdown 表格列出对比维度
+ ### 对比总览
+ 直接使用 <etf_raw_data> 中已提供的"ETF 对比数据表格"，一字不改地放入此章节。
+ 禁止修改表格结构、增删行或列。
```

同时移除 v4 的"格式纪律"（表格约束现在由代码保证，不需要 Prompt 约束——减少 Prompt 复杂度）。

#### 改动 E：System Prompt 注意事项收敛（4A 修复）

```diff
- - 历史业绩不代表未来表现
+ - 历史业绩不代表未来表现（此条仅写这一句，不展开、不附加解释）
```

#### 改动 F：跟踪标的页面抓取（4B 修复）

**`_shared.py`** — `fetch_fund_detail` 新增正则从 f10 页面直接提取：
```python
m_track = re.search(r"跟踪标的</td><td>([^<]+)</td>", html)
if m_track:
    result["tracking_index"] = m_track.group(1).strip()
```

**`basic.py`** — 抓取值覆盖匹配值：
```python
entry["tracking_index"] = detail.get("tracking_index") or entry["tracking_index"]
```

**INDEX_CODE_MAP** 补充：
```python
"证券公司指数": "sz399975",
"中证酒指数":   "sz399987",
```

**效果**：证券ETF国泰（512880）→ "证券公司指数"（跟踪误差可计算）、酒ETF鹏华（512690）→ "中证酒指数"（跟踪误差可计算）。

---

### 完整 Prompt 文本

**System Prompt（v5 完整版）：**

```
你是一位 ETF 研究员，擅长把复杂数据翻译成普通投资者能看懂、能用来做决策的白话解读。你的任务是：帮用户看清几只 ETF 之间真正重要的差异是什么，让用户能结合自身投资偏好做出判断。如果数据不足以支撑某个结论，坦诚说明，不强行给结论。

## 行为准则

1. 禁止提供投资建议和产品层优劣判断。不说"建议买入"、"推荐持有"、"这只更好"、"明显更优"、"XXX 是更好的选择"等结论性表述。只能陈述客观事实（如"费率低 0.1%"），不能做价值判断（如"因此更值得购买"）

2. 禁止编造数据。只基于提供的真实数据，缺失数据标注"暂无数据"

3. 底部必须标注："数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考"

4. 结构化输出，格式见下方

5. 最多对比 5 只 ETF

## 术语指南

报告中首次出现以下术语时，必须用一句白话解释：
- 跟踪误差：反映 ETF 复制指数的精准度，越小越准。被动 ETF 通常 < 0.5%，若 > 3% 需特别说明风险
- 综合费率：管理费 + 托管费（两者每年从净值中扣除），是持有 ETF 的核心成本。两只 ETF 费率相差 0.1% 以上时，应计算 5 年累计成本差异
- 管理费率、托管费率、成立日期出现时也需简短白话解释

## 分析流程（内部思考，不输出给用户）

生成报告前，先在心里完成以下分析：
1. 识别核心矛盾：这几只 ETF 之间"最值得关注的分化点"是什么？
   - 可能是费率差距、规模差距、行业/指数的本质差异、或业绩分化
2. 根据核心矛盾，决定本次报告的侧重点和篇幅分配
3. 确定注意事项中需要特别提示的风险点（必须针对本次对比结果，禁止使用通用模板）

## 输出格式（严格遵守）

**输入只有 1 只 ETF 时（深度解读模式）：**
生成「深度解读报告」，包含：
- 该 ETF 的基本画像（跟踪什么指数、什么行业、哪家基金公司）
- 各项指标意味着什么（费率水平、规模大小、跟踪误差、近期业绩表现分别说明）
- 该 ETF 的潜在关注点（如规模偏小 → 流动性风险、成立时间短 → 历史数据有限等，必须基于数据）

**输入 2-5 只 ETF 时（对比模式）：**

### 对比总览
直接使用 <etf_raw_data> 中已提供的"ETF 对比数据表格"，一字不改地放入此章节。禁止修改表格结构、增删行或列。

### 逐只解读
每只 ETF 一段话（不超过 5 行），突出该 ETF 在本轮对比中最显著的特点

### 维度对比总结
围绕核心矛盾，选择差异最大的 2-3 个维度深入对比

### 注意事项
- 历史业绩不代表未来表现（此条仅写这一句，不展开、不附加解释）
- 根据本次对比结果，列出真正需要注意的风险点（禁止使用通用模板内容）

---

数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考。不构成投资建议。
```

**User Prompt（v5 完整版）：**

```jinja
当前实际日期为 {{ current_date }}。请以此为基准判断数据中的日期是否正常，不要将 2026 年的日期误判为"未来日期"。

{% if has_data == "false" %}
请直接告知用户：{{ error_message }} 严禁编造任何数据。
{% else %}
{% if warning %}
> ⚠️ 重要提醒：{{ warning }}
>
> 请在报告的「注意事项」章节开头单独标注此提醒。
{% endif %}

请严格基于下方数据生成报告。全程遵守 SYSTEM 中的行为准则和输出格式要求。
严禁脱离给定数据编造行情、收益、风险等未给出的信息。

<etf_raw_data>
{{ comparison_data }}
</etf_raw_data>
{% endif %}
```

---

### 修改前后对比

#### C1（510300 沪深300ETF华夏）— 表格稳定 + 百分比格式修复

首测发现：聚合节点预渲染表格时，涨跌幅小数直出（`+0.0166` 而非 `+1.66%`）。修复：聚合节点格式化 `f"{val*100:+.2f}%"`（commit `5e4951a`）。重测通过——表格 17 行 × 1 列完整、百分比格式正确、无列丢失无转置。

#### C2（159995 芯片ETF华夏 + 512690 酒ETF鹏华）— 跟踪指数/误差修复 + 性能优化

修复前：酒ETF跟踪指数、跟踪误差均为"暂无数据"；整体响应超过 1 分钟。

**修复 1：跟踪标的正则 bug**（commit `3d1bc3c`）
- 根因：f10 页面结构是 `<th>跟踪标的</th><td>中证酒指数</td>`，原正则写 `</td><td>` 永远匹配不上
- 修复：正则改为 `跟踪标的</th><td>`，本地实测 512690 提取到"中证酒指数"

**修复 2：性能优化**（commit `9bb30be`）——三处串行瓶颈并行化：
- basic 节点：多 ETF 详情抓取改为并发（原串行最坏 80s+）
- `fetch_fund_detail`：pingzhongdata（500KB）与 f10 页面并行下载
- `_fetch_single_performance`：两阶段并行（K线+跟踪标的 → 指数K线+净值），单只最坏从 75s 降到 ~6s
- 跟踪标的优先从 f10 页面（44KB）提取，2MB 基金列表降级为兜底；基金列表缓存 10min→1h
- 实测：basic 两只 ~5.5s，performance 两只 ~6.5s（修复前各最坏 80s/75s）

**修复 3：国证芯片指数代码纠错**（commit `9bb30be` 内）
- 误用 `sz399995`，实测新浪该代码实为"基建工程"；国证芯片正确行情代码为 `sz980017`（已验证可用）
- 修正后 159995 跟踪误差 0.43%（合理），修复前会算出 45% 的荒谬值

**修复 4：成交额口径错误**（commit `5b7f808`，⚠️ 改了聚合节点需重新导入 DSL）
- 根因：聚合节点用 `s.get("volume")`（成交量/股）当"今日成交额"，实为 `amount`（成交额/元）
- 实测差异：159995 成交量 6.31 亿股 vs 真实成交额 7.55 亿元；512690 成交量 15.10 亿股 vs 真实成交额 6.68 亿元
- **影响**：C2 中"酒ETF 成交额 15.10 亿 > 芯片 6.31 亿"的解读结论完全相反（真实是芯片 7.55 亿 > 酒 6.68 亿）——口径错误会直接误导"交易活跃度"维度结论
- 修复：`s.get("volume")` → `s.get("amount")`

**修复 5：行业分类缺失"酒"**（commit `5b7f808`）
- 根因：`infer_industry` 消费组没有"酒"关键词，酒ETF 落入"综合/其他"（LLM 反而自己推断成"消费类"）
- 修复：消费组补"酒"，实测 酒ETF→消费、芯片ETF→科技/TMT、证券ETF→金融

**C2 重测结果（V5）**：跟踪指数=国证半导体芯片指数/中证酒指数 ✓，跟踪误差=0.43%/1.46% ✓，行业分类=科技/TMT/消费 ✓，成交额=7.55/6.68 亿元（口径修正后）✓；表格 17 行完整；逐只解读、维度对比、注意事项均基于新数据，无模板化发散；响应时长显著下降（数据层 ~8s，用户未再反馈超时）。

**C2 重测质量评审（V5，成交额修正后）**：

数据层全部正确。报告质量整体良好，但仍发现 4 个 LLM 推断/Hallucination 问题：

| # | 问题 | 原文 | 严重度 |
|---|------|------|--------|
| 1 | 成立日期时长算错 | "成立日期为 2019-04-04，运作时间约**一年半**" — 实际 2019-04 至今已 **7 年** | **P1** 事实性错误 |
| 2 | 无数据支撑的特征定性 | "属于消费板块**防御性品种**" — 数据中无"防御性"依据，模型自行推断 | P2 |
| 3 | 有限数据强行推形态 | "芯片ETF呈现'**长时间横盘 + 短期急跌**'的特征" — 只有近N期点收益率，看不出横盘形态，属幻觉 | P2 |
| 4 | 预测性表述 | "**如果市场延续当前风格，价格可能继续大幅震荡**" — 行为准则已禁投资建议但未覆盖此类条件预测 | P2 |

**根因分析**：
- #1：LLM 自行做算术（2019→2026）出错。成立日期不应由 LLM 计算时长，直接展示即可。
- #2-4：System Prompt 行为准则「禁编造数据」覆盖的是凭空捏造数据值，但对"基于有限数据点做合理推断"的场景约束不足。LLM 拿到 5 个时间点收益率后就试图画走势轮廓——这不是恶意编造，而是过度推理。
- 这类问题在 **CoT 模式下可能更易出现**：模型被鼓励"识别核心矛盾"后，会在总结段落中填补逻辑链条，而填补过程缺乏数据锚点。

**v5.1 候选修复**：
- System Prompt 行为准则加一条：「禁止基于有限数据点做趋势推断、形态描述或特征定性（如"横盘""急跌""防御性品种"等）。只陈述数据本身，不推断数据背后未显示的规律。」
- 术语指南的"成立日期"解释中去掉引导 LLM 复述的暗示，改为：「成立日期 — 直接引用，无需计算运作时长。」
- #4 预测性表述可归入已有行为准则"禁止投资建议"，但当前措辞偏窄（只禁"买入/卖出/持有"建议），v5.1 可扩展为「禁止任何预测性、建议性表述（含条件假设）」。
- **注意**：这些是 LLM 行为约束，只改 System Prompt（DSL），不动 API 代码。

**C2 第三次重测（指令泄露 + 跟踪误差误判）：**

用户在手动重跑 C2 后提交了第三次输出，发现两个新问题：

| # | 问题 | 严重度 | 现象 |
|---|------|--------|------|
| 5 | System Prompt 指令泄露 | **P0** | 报告第一行输出了 `"直接使用 <etf_raw_data> 中已提供的"ETF 对比数据表格"，一字不改地放入此章节。"` — System Prompt 中给 LLM 的操作指令被当作报告内容原样输出 |
| 6 | 跟踪误差阈值误导 | **P1** | 术语指南"被动 ETF 通常 < 0.5%"不区分宽基/行业 ETF，导致 LLM 将酒 ETF 1.46% 误判为"明显偏高"、"接近警戒线"。实际上行业 ETF（白酒、半导体等）跟踪误差 0.5%-2% 均为正常区间 |

**修复 #5（已应用）**：格式纪律加隔离层
```
- 下方内容是报告结构规则，不是需要抄写的模板文本。严禁将规则说明复制到输出中
```
同时移除已过时的"表格列不可变"条目（表格现在由代码保证）。

**修复 #6（已应用）**：术语指南跟踪误差重写
```
跟踪误差：衡量 ETF 净值贴合标的指数的波动稳定程度。
判断标准因 ETF 类型而异：
- 宽基 ETF（沪深300、上证50等）通常 < 0.5%
- 行业/主题 ETF（半导体、白酒、军工等）因成分股权重集中、调仓成本等因素，
  跟踪误差中枢天然更高（0.5%-2% 均为合理区间）
评价时请结合 ETF 的行业类型判断，不要一律套用 0.5% 标准
```

**C3（510300 单 ETF 深度解读）— 第三次重测**：

#7 (P1) User Prompt 指令泄露已修复 ✓（日期指令加 `(内部上下文，不必输出到报告中)` 前缀，commit `055f6b5`，需重新导入 DSL）。报告不再输出"当前实际日期为…"。

遗留 3 个 P2 级瑕疵（v5.1 候选）：
| # | 问题 | 原文 | 严重度 |
|---|------|------|--------|
| 8 | 注入无关领域知识 | "与标普编制的'标普中国500'等无关"——无人提及标普，模型自己加戏 | P2 |
| 9 | 臆造竞品参照 | "部分同类产品已降至 0.15%（费率）""跟踪误差低于 0.2% 的产品"——单 ETF 模式下无从对比，凭空制造参照系 | P2 |
| — | 结构漂移 | 新增"概括"段落、拆出独立"近期业绩表现"章节，偏离三要素简洁格式 | P2 |

根因：单 ETF 模式下模型觉得"内容不够丰富"，自主补背景知识和制造参照系来填篇幅。修复方向：行为准则禁推断/禁偏离数据。

**C4（5 只 510300/510310/159915/512880/588000）**：

表格 17×5 完整，跨行业归因清晰，维度消解到位（告知跟踪误差门槛应区分宽基/行业 ETF）。无新增问题。

**C5（999999 无效代码）**：

聚合节点报 `Output current_date is missing.`。根因：错误分支 return 漏了 `current_date` 字段（正常分支有），DSL 声明了该 output 必返。修复：错误分支补 `"current_date": ""`（commit `bbcb12b`，需重新导入 DSL）。重测通过——输出"代码 999999 无效，请检查后重试。" ✓

---

### v5 全量测试总结

| Case | 场景 | 结果 | 关键修复 |
|------|------|------|----------|
| C1 | 单只宽基 | ✓ | 百分比格式、指令泄露修复验证 |
| C2 | 跨行业 2 只 | ✓ （三次重测逐步收敛） | 跟踪标的正则+抓取、性能并行化、成交额口径、行业分类、跟踪误差阈值 |
| C3 | 单 ETF 深度解读 | ✓ （P2 瑕疵遗留） | User Prompt 指令泄露修复 |
| C4 | 5 只边界 | ✓ | 表格完整性、维度过载处理 |
| C5 | 无效代码 | ✓ | current_date 字段补齐 |

### 剩余待修复（→ 已全部迁入 v6）

以上 7 个问题已收敛为 v6 的 4 处 System Prompt 改动，详见上方 v6 章节。


---

## v4 — 2026-08-11（P0 兜底修复 + 数据层准确性问题）

### 触发原因

v3 全量测试（C1~C5）发现 5 项已修复 + 4 个新问题。按**准确性 > 有用性 > 一致性/鲁棒性**的评估优先级，本版集中修复 3 个 P0 和 2 个 P1：

| 优先级 | 问题 | 首次出现 case | 所属层级 |
|--------|------|--------------|----------|
| P0 | **跟踪误差单位歧义 + 数据源噪声** — 聚合节点输出裸数字 `0.0314`，v2 解读为 3.14%（偏高）、v3 解读为 0.0314%（极低），结论完全相反 | C1 | 数据层 |
| P0 | **has_data 错误路径从不断生效** — 聚合节点返回 `has_data="false"` 但 LLM 节点未注册该 Jinja2 变量，`{% if has_data == "false" %}` 永远为 False，999999 生成空壳报告 | C5 | Infra 层 |
| P0 | **跟踪误差虚高 7 倍** — 改用净值算后仍得 2.53%（Choice 参考值 0.35%），根因是 `Data_netWorthTrend.y` 为未复权单位净值，拆分/分红日断崖跳变污染日收益率 | C1（部署后验证） | 数据层 |
| P1 | **输出结构漂移** — CoT 强化推理后模型自主增删章节（C2 拆出独立收益表、丢今日涨跌幅行） | C2 | Prompt 层 |
| P1 | **建议性措辞冒头** — 5 只高信息量输入下出现"510300 明显更优" | C4 | Prompt 层 |

### 方法论讨论

v4 改动涉及 4 个架构决策，面试中可能被追问：

**决策 1：为什么不用 Few-shot 而坚持 Zero-shot + CoT？**

Few-shot 的收益在于锚定风格，但代价很大：每轮多 1500-2500 Token × 数据已很长（5 只 ETF × 15 字段），可能逼近上下文窗口。而且范例本身需要持续维护——数据格式、API 字段一变，范例就得跟着改。CoT 的三步推理（识别核心矛盾 → 决定篇幅分配 → 确定风险点）零 Token 增量、零维护成本，效果已在 v3 得到验证（C2 归因分离、C4 维度消解），没必要再加 Few-shot。

**决策 2：跟踪误差到底用什么算？**

三种口径：
- **场内价格 vs 指数**（v2）：含折溢价噪声，510300 算出 3.14%，v2/v3 的术语指南阈值（< 0.5% 正常，> 3% 预警）无法匹配
- **基金净值 vs 指数**（v4 初版）：理论上正确，但 `Data_netWorthTrend.y` 是未复权净值，拆分点跳变 → 虚高到 2.53%
- **equityReturn 复权日收益率 vs 指数**（v4 终版）：✅ 东方财富已内置复权修正，直接拿 pre-computed 的日收益率，避免自己拼装数据引入误差。结果与 Choice 终端仅差 2bp

**决策 3：聚合节点格式化的"关键少数"原则**

聚合节点是 LLM 看到的数据的"唯一原文"。这里少加一个 `%` 符号，LLM 就会在 3.14% 和 0.0314% 之间摇摆。所以聚合节点的责任不是"传原始数据"，而是"保证传递给 LLM 的每一个数字都带明确的语义单位"。跟踪误差的 ×100 + `%` 操作、规模加"亿元"后缀、涨跌幅加 `±` 号，都是这个原则的体现。

**决策 4：前端为什么要标注跟踪误差口径？**

同一个 ETF，不同数据商披露的"跟踪误差"可以是 0.35%（净值口径）或 3%（场内价格口径）——两者差一个数量级。用户在别的平台看到 0.35%，在我们这里看到 2.53% 就会认为我们数据错了（实际上也确实错了）。前端标注"按行业通行口径计算：基于基金单位净值"解决两个问题：①用户知道我们用的什么口径 ②面试时证明你对数据口径差异有工业级认知。

**决策 5：P0/P1/P2 的修复边界**

v4 集中修 P0 + P1，P2（日期动态注入）延后——原因：P2 不是"错"，只是"不够好"；而 P0（跟踪误差虚高、错误路径不生效）是"错的"，必须先修。另外 P2 需要改聚合节点输出一个新的动态变量，和改动 A 在同一节点，下一轮一起做可避免反复重新导入 DSL。

### 修改了什么

**改动 A（数据层）：跟踪误差从"场内价格"切换到"复权净值日收益率"**

`api/etf/_shared.py` 新增 `fetch_etf_nav()` + 修改 `_fetch_single_performance`：

```python
# 改前：场内价格 vs 指数 K 线
result["tracking_error"] = calculate_tracking_error(data, index_kline)

# 改后：fetch_etf_nav 从 pingzhongdata JS 提取 equityReturn（已复权日收益率），构建伪收盘价序列
nav_data = fetch_etf_nav(code)  # 用 equityReturn / 100 逐日累积，而非直接用 y 字段
result["tracking_error"] = calculate_tracking_error(nav_data, index_kline) if nav_data else None
```

`fetch_etf_nav` 的关键逻辑：
```python
prev_close *= (1 + eq_ret / 100)  # equityReturn 是百分比，含拆分红修正
```

**改动 B（数据层）：聚合节点跟踪误差 ×100 加 `%`**

```python
elif key == "跟踪误差":
    val = f"{val*100:.2f}%"  # 0.0035 → 0.35%
```

**改动 C（Infra 层）：has_data 变量注册 + 错误类型区分**

LLM 节点 `jinja2_variables` 补注册：
```yaml
- value_selector: ['1785916505938', 'has_data']
  variable: has_data
```

聚合节点区分两种错误：
```python
all_invalid = all(basic.get(c) and basic[c].get("error") == "invalid_code" for c in cleaned_codes)
if all_invalid:
    msg = f"代码 {'、'.join(cleaned_codes)} 无效，请检查后重试。"
else:
    msg = "当前数据服务暂不可用，请稍后重试。"
```

User Prompt 错误路径简化为：
```jinja
{% if has_data == "false" %}
请直接告知用户：{{ error_message }} 严禁编造任何数据。
{% endif %}
```

**改动 D（Prompt 层）：输出格式纪律**

System Prompt 新增「格式纪律」段落（CoT → 输出格式 之间）：
```
**格式纪律**：洞察力 + 格式漂移 = 不合格。一份优秀的报告必须在规定格式内展示洞察。
- 章节不可增删：只能使用下方列出的章节，不可新增、不可省略
- 表格列不可变：对比总览表格的列按下方规定，不可增删
- 标题不可自创：标题使用下方规定名称，不可自行发挥
```

**改动 E（Prompt 层）：行为准则扩展——禁"产品层优劣判断"**

```diff
- 1. 禁止提供投资建议。不说"建议买入"、"推荐持有"、"这只更好"等结论性判断
+ 1. 禁止提供投资建议和产品层优劣判断。不说"建议买入"、"推荐持有"、"这只更好"、"明显更优"、"XXX 是更好的选择"等结论性表述。只能陈述客观事实（如"费率低 0.1%"），不能做价值判断（如"因此更值得购买"）
```

**改动 F（前端层）：报告底部标注跟踪误差计算口径**

`index.html` 在 result-card 和 footer 之间新增：
```
本报告中"跟踪误差"按行业通行口径计算：基于基金单位净值（而非场内交易价格），逐日计算 ETF 净值收益率与标的指数收益率的偏离，年化得出。该口径剔除了二级市场折溢价噪声，与基金经理实际运作水平直接相关。
```

### 修改前后对比

⏳ 待 Dify 重新导入 DSL 后，以 C1~C5 固定测试用例集验证。

#### C1：510300,510310（同行业沪深300 ETF）→ 考察跟踪误差准确性 + 注意事项动态化

**验证结果**：数据层修复 ✅ 生效（跟踪误差 0.37%/0.36%，对齐 Choice 参考值 0.35%/0.34%）；格式纪律 ⚠️ 部分生效；建议措辞 ✅；注意事项 ✅。

| 对比维度 | v3（数据层已修复） | v4 | 结论 |
|----------|--------------------|----|------|
| 跟踪误差数值 | 0.37% / 0.36% | 0.37% / 0.36% | ✅ 与 Choice 对齐，v2 的 2.53%/2.03% 已修复 |
| 结构漂移 | 出现「关于费率的说明」独立章节、术语解释散落 | 合并「关于费率的说明」入维度对比总结、术语解释集中 | ✅ 格式纪律生效 |
| **表格完整性** | 含「今日涨跌幅」行 | **丢了「今日涨跌幅」行** | ⚠️ 新问题：格式纪律未规定必选维度清单，模型自选列导致丢数据 |
| 建议性措辞 | "华泰柏瑞的流动性优势是明显的" | "易方达的盘口深度可能不足" | ✅ 客观化 |
| 注意事项 | 4 条针对性 | 4 条针对性 + 期限结构分化分析 | ✅ 动态化 |
| 核心矛盾 | "同质化产品之间的'流动性'与'细节差异'" | "底层资产几乎完全一致，真正差异在规模/流动性和业绩分化" | ✅ 更简洁 |

**新发现（P1 候选）**：v4 对比总览丢失「今日涨跌幅」行。根因：格式纪律写了"表格列不可变"，但输出格式未列出**必选维度清单**，模型把"列出对比维度"理解为可自选列，v4 精简掉了今日涨跌幅。这比格式漂移更隐蔽——不崩格式但悄悄丢数据。v5 候选：输出格式明确列出对比总览必选维度（今日涨跌幅、今日成交额、近N周/月/年收益等）。

**附注**：本次对比 v3/v4 输出中跟踪误差均为 0.37%，因为数据层修复在 Vercel 端生效（与 Prompt 无关），两版 Prompt 共用同一份修复后数据。v3/v4 输出差异反映的是 Prompt 层改动（格式纪律、建议措辞、核心矛盾）的效果。

#### C2：159995,512690（跨行业：半导体 vs 酒）→ 考察结构漂移是否收敛 + 行业归因

**验证结果**：结构漂移 ✅ 收敛；行业归因 ✅；建议措辞 ✅；表格完整性 ⚠️ 再次丢列（C1 丢「今日涨跌幅」，C2 丢「管理/托管费率」）。

| 对比维度 | v3 | v4 | 结论 |
|----------|----|----|------|
| 结构漂移 | 四段式完整 | 四段式完整 | ✅ 无独立「收益表现对比」章节，P1-1 收敛 |
| 行业归因 | 只说"完全不同的波动节奏" | "直观体现了**不同行业**在同一市场环境下的不同表现节奏，这是两者最核心的差异" | ✅ 归因分离显式化 |
| 表格完整性 | 含管理/托管费率 | **丢了管理费率、托管费率两行** | ⚠️ 列丢失不稳定（C1 丢今日涨跌幅，C2 丢管理/托管费率） |
| 术语解释 | 散落正文 | 压缩为一行"术语白话解释：" | ✅ 更精炼 |
| 建议措辞 | "可能与近期价格大幅波动导致观望情绪有关"（猜测性） | "芯片ETF持有人**可能**更偏向中长期配置"（标注可能） | ✅ 客观化 |
| 注意事项 | 4 条针对性 | 4 条针对性 | ✅ |

**确认（P1 必修）**：列丢失问题两次复现且丢失对象不同——C1 丢「今日涨跌幅」，C2 丢「管理/托管费率」。根因确认：格式纪律的"表格列不可变"未列出**必选维度清单**，模型自选列的精简对象每次不同，输出不稳定。v5 候选：输出格式明确列出对比总览必选维度。

#### C3：510300（单只 ETF）→ 考察深度解读模式 + 跟踪误差口径

**验证结果**：深度解读模式 ✅；跟踪误差口径 ✅（0.37%）；建议措辞 ✅；场景锚定 ✅。

| 对比维度 | v3 | v4 | 结论 |
|----------|----|----|------|
| 深度解读触发 | 正确触发（隐式信号：数据仅 1 条） | 正确触发 | ✅ P1-1 验证通过 |
| 结构 | 三要素 + 发散扩展（4 个小点） | 三要素（画像/指标/关注点），更收敛 | ✅ 格式纪律约束下更贴近规定 |
| 跟踪误差 | 0.37%，"远低于 3% 警戒线" | 0.37%，"处于合格范围" | ✅ 数据层修复对齐 Choice |
| 场景锚定 | "特别适合机构资金和中长期配置型投资者" | "每持有 10,000 元，每年产生约 20 元持有成本" | ✅ v4 把费率翻译成绝对金额，更可感知 |
| 建议措辞 | "特别适合机构资金…" | "需考虑自己的风险承受能力是否匹配" | ✅ 决策权交还用户 |
| 模板化尾巴 | 无 | 结尾新增一句"投资有风险，入市需谨慎" | ⚠️ 低优：P0 模板化的轻微回潮，仅一句，不构成复发 |

**观察（低优）**：v4 关注点提到"国内 ETF 市场近年费率竞争激烈"——属模型自带领域知识（非给定数据），不算编造但属"脱离数据"边缘，观察即可。

#### C4：510300,510310,159915,512880,588000（5 只边界）→ 考察"明显更优"消失 + 格式纪律

**验证结果**："明显更优" ✅ 消失；核心矛盾 ✅ 历次最佳；格式纪律 ⚠️ 章节无漂移但表格转置；表格完整性 ⚠️ 丢列最严重（9/16 维度）。

| 对比维度 | v3 | v4 | 结论 |
|----------|----|----|------|
| "明显更优" | 无 | 无，"以948.72亿元规模居首" | ✅ 改动 E 生效 |
| 表格结构 | 维度作行（16 行），全列 | **转置为 ETF 作行（5 行），只留 9 个维度** | ⚠️ 自主转置表格 + 丢 7 列（管理/托管费率、最新价、今日成交额、近1周/近1月/近6月） |
| 核心矛盾 | "业绩分化是核心矛盾" | "**底层指数不同，导致业绩与风险特征差异极大，而非基金产品本身的优劣**" | ✅ 归因分离 + 避免产品优劣判断，一句话双达成 |
| 注意事项 | 6 条针对性 | 5 条针对性（含数据缺口、回撤风险、费率累积） | ✅ |
| 场景锚定 | 无 | "持有 100 元证券ETF，5 年比 0.2% 费率产品多扣约 2 元" | ✅ |

**关键发现（P1 必修升级）**：列丢失问题**第三次复现且最严重**——C1 丢 1 列、C2 丢 2 列、C4 丢 7 列。v4 还自主转置了表格（维度作行 → ETF 作行）。根因：格式纪律只约束"章节不可增删"，未约束"表格结构（转置方向、必选维度）"。模型在不崩章节的前提下自由重构表格，数据完整性无法保证。v5 必须：①输出格式列出对比总览必选维度清单 ②明确表格形态（维度作行 / ETF 作列）。

#### C5：999999（无效代码）→ 考察 has_data 错误路径

**验证结果**：✅ **完美通过**。has_data 错误路径真正生效（v4 唯一版本，v2/v3 均无此能力——v2/v3 时代该分支从未被触发过）。

v4 输出：
```
代码 999999 无效，请检查后重试。
数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考。不构成投资建议。
```

| 验证点 | v2/v3（修复前） | v4（修复后） |
|--------|----------------|--------------|
| has_data 分支 | 从未触发（变量未注册，`{% if has_data == "false" %}` 恒为 False） | ✅ 触发错误路径 |
| 输出 | 生成空壳报告（格式框架 + 全"暂无数据"） | ✅ 简洁一句话"代码 999999 无效，请检查后重试。" |
| 错误类型区分 | 无（统一"数据服务暂不可用"） | ✅ 聚合节点区分 invalid_code vs 服务不可用 |
| 编造风险 | 空壳报告诱导模型编造"暂无数据"项 | ✅ 错误路径明文"严禁编造任何数据" |

**架构说明**：此修复涉及三层配合——聚合节点（区分错误类型 + 生成具体 error_message）→ LLM 节点配置（jinja2_variables 补注册 has_data 变量）→ User Prompt（错误路径简化为"请直接告知用户：{{ error_message }}")。缺少任何一层，错误路径都无法正确工作。

### 代价 / 副作用

| # | 风险 | 缓解 |
|---|------|------|
| 1 | **格式纪律可能压制 CoT 正面效果** — C4 的"维度消解"（告诉用户哪些维度不用纠结）正是在现有格式内完成的，硬约束可能让模型缩手缩脚 | 措辞强调"在 **规定格式内** 展示洞察"，不禁止思考，只禁止结构越界。C4 测试重点观察 |
| 2 | **深度解读模式仍依赖隐式触发** — 单只 ETF 靠 `<etf_raw_data>` 里只有 1 条数据来隐式切换模式，Prompt 没有显式规则 | 当前可行（C3 v3 验证通过），但不如显式。v5 候选：User Prompt 加 `{% if etf_count == 1 %}` 分支 |
| 3 | **聚合节点错误区分依赖 basic 返回的 `error` 字段** — 如果 basic 返回 `{}`（空对象而非带 error 字段），`all_invalid` 会误判 | 条件 `basic.get(c) and basic[c].get("error") == "invalid_code"` 中 `basic.get(c)` 先防御空值，安全 |
| 4 | **equityReturn 第一个数据点为 0** — 构建伪收盘价时第一条无前值，跳过即可 | `if eq_ret == 0 and prev_close == 1.0: continue` |
| 5 | **伪收盘价序列可能因浮点累积误差而漂移** — 3469 条数据逐日 `prev_close *= (1 + eq_ret/100)` 可能累积误差 | ETF 跟踪误差关注的是日收益率差异（非绝对值），`calculate_tracking_error` 用日收益率而非水平值，累积误差不影响计算结果 |

### 完整 Prompt 文本

**System Prompt（v4 完整版）：**

```
你是一位 ETF 研究员，擅长把复杂数据翻译成普通投资者能看懂、能用来做决策的白话解读。你的任务是：帮用户看清几只 ETF 之间真正重要的差异是什么，让用户能结合自身投资偏好做出判断。如果数据不足以支撑某个结论，坦诚说明，不强行给结论。

## 行为准则

1. 禁止提供投资建议和产品层优劣判断。不说"建议买入"、"推荐持有"、"这只更好"、"明显更优"、"XXX 是更好的选择"等结论性表述。只能陈述客观事实（如"费率低 0.1%"），不能做价值判断（如"因此更值得购买"）

2. 禁止编造数据。只基于提供的真实数据，缺失数据标注"暂无数据"

3. 底部必须标注："数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考"

4. 结构化输出，格式见下方

5. 最多对比 5 只 ETF

## 术语指南

报告中首次出现以下术语时，必须用一句白话解释：
- 跟踪误差：反映 ETF 复制指数的精准度，越小越准。被动 ETF 通常 < 0.5%，若 > 3% 需特别说明风险
- 综合费率：管理费 + 托管费（两者每年从净值中扣除），是持有 ETF 的核心成本。两只 ETF 费率相差 0.1% 以上时，应计算 5 年累计成本差异
- 管理费率、托管费率、成立日期出现时也需简短白话解释

## 分析流程（内部思考，不输出给用户）

生成报告前，先在心里完成以下分析：
1. 识别核心矛盾：这几只 ETF 之间"最值得关注的分化点"是什么？
   - 可能是费率差距、规模差距、行业/指数的本质差异、或业绩分化
2. 根据核心矛盾，决定本次报告的侧重点和篇幅分配
3. 确定注意事项中需要特别提示的风险点（必须针对本次对比结果，禁止使用通用模板）

## 输出格式（严格遵守）

**格式纪律**：洞察力 + 格式漂移 = 不合格。一份优秀的报告必须在规定格式内展示洞察。
- 章节不可增删：只能使用下方列出的章节，不可新增、不可省略
- 表格列不可变：对比总览表格的列按下方规定，不可增删
- 标题不可自创：标题使用下方规定名称，不可自行发挥

**输入只有 1 只 ETF 时（深度解读模式）：**
生成「深度解读报告」，包含：
- 该 ETF 的基本画像（跟踪什么指数、什么行业、哪家基金公司）
- 各项指标意味着什么（费率水平、规模大小、跟踪误差、近期业绩表现分别说明）
- 该 ETF 的潜在关注点（如规模偏小 → 流动性风险、成立时间短 → 历史数据有限等，必须基于数据）

**输入 2-5 只 ETF 时（对比模式）：**

### 对比总览
用 Markdown 表格列出对比维度

### 逐只解读
每只 ETF 一段话（不超过 5 行），突出该 ETF 在本轮对比中最显著的特点

### 维度对比总结
围绕核心矛盾，选择差异最大的 2-3 个维度深入对比

### 注意事项
- 历史业绩不代表未来表现
- 根据本次对比结果，列出真正需要注意的风险点（禁止使用通用模板内容）

---
数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考。不构成投资建议。
```

**User Prompt（v4 完整版，Jinja2 模板）：**

```jinja
当前实际日期为 2026 年 08 月。请以此为基准判断数据中的日期是否正常，不要将 2026 年的日期误判为"未来日期"。

{% if has_data == "false" %}
请直接告知用户：{{ error_message }} 严禁编造任何数据。
{% else %}
{% if warning %}
> ⚠️ 重要提醒：{{ warning }}
>
> 请在报告的「注意事项」章节开头单独标注此提醒。
{% endif %}

请严格基于下方数据生成报告。全程遵守 SYSTEM 中的行为准则和输出格式要求。
严禁脱离给定数据编造行情、收益、风险等未给出的信息。

<etf_raw_data>
{{ comparison_data }}
</etf_raw_data>
{% endif %}
```

---

## v3 — 2026-08-11（Prompt 工程重构）

### 触发原因

从 v2 开始积累的 5 个已知问题全部纳入本轮修复范围：

| 优先级 | 问题 | 根因 |
|--------|------|------|
| P0 | 注意事项模板化 | System Prompt 写死了两条固定免责，模型不思考"本次对比真正需要提醒什么" |
| P0 | 输出风格跑偏 | Zero-shot 无角色锚定，模型在学术风/口语化之间摇摆 |
| P1 | 跨行业对比差异弱 | 输出格式未要求模型"先识别核心矛盾再组织报告" |
| P1 | 单只 ETF 无行为规范 | 输出格式只有对比模式，未覆盖 1 只 ETF 场景 |
| P2 | 日期上下文写死 | 日期嵌在 System Prompt 而非 User Prompt，不利于未来动态注入 |

### 方法论：四步 Prompt 工程流程

本次迭代严格遵循以下流程（详见团队讨论记录）：

```
第一步：明确目标 & 成功标准
第二步：拆解 Prompt 结构（角色 / 任务 / 要求 / 细节）
第三步：选 Prompt 类型（Zero-shot / Few-shot / CoT / ReAct / CoVe / Structured Output）
第四步：调优闭环（修改 → 测试 → 收集 → 对比 → 归因 → 迭代）
```

**关键决策讨论：**

1. **成功标准优先级**：经讨论确定为「准确性 > 决策有用性 > 一致性/鲁棒性 > 成本」。理由：前端已做输入校验，格式一致性不是主要风险；准确性是底线（金融场景幻觉=事故）；核心价值是「最后一公里决策辅助」。评估手段采用 A1（人工评测打分）+ A3（代码化指标：事实准确率/幻觉率/格式合规率），暂不引入 LLM-as-Judge（A2）以控制变量。

2. **"决策有用性"的操作化定义**：拆为 3 个子维度——①对比维度有主次（自动识别核心矛盾，非平均用力）②结论有场景锚定（"费率低 → 长期持有 5 年省 X 元 → 长线投资者值得关注"）③不确定性能坦诚表达（数据不足时不强行给结论）。

3. **为什么选 Zero-shot + CoT 而非 Few-shot**：Few-shot 的 token 成本高（每个范例 1500-2500 token）、范例需要随数据格式变更持续维护、且可能限制模型在边缘场景的泛化。CoT 通过 3 步内部推理（识别核心矛盾 → 分配篇幅 → 定制注意事项）直接命中 P0+P1 问题，零额外 token 开销，不满意可无成本回滚。

4. **System Prompt vs User Prompt 重新分工**：核心原则——System Prompt 是"宪法"（角色、行为准则、术语规则、输出格式），User Prompt 是"每日简报"（日期上下文、本轮数据、异常分支）。日期上下文从 System 迁移到 User Prompt，为 P2 动态注入做准备。

5. **Prompt 结构拆解**：按「角色 → 任务 → 要求 → 细节」四层审视 v2，发现 5 个结构性问题——角色太泛 / 任务与业务目标脱节 / System-User 分工模糊 / 单只 ETF 缺场景分支 / 术语解释与行为红线混淆。本轮全部修复。

### 修改了什么

| # | 改动 | 位置 | 对应结构层级 | 修复的 P 问题 |
|---|------|------|-------------|--------------|
| ① | 角色 + 任务重写：「ETF 研究员」视角 + 决策导向任务描述 | System 开头 | 角色 / 任务 | P0 风格跑偏 |
| ② | 术语解释从行为准则拆出，独立为「术语指南」段落 | System 中部 | 细节 | 结构优化 |
| ③ | 新增 CoT 三步骤：「内部分析流程」→ 识别核心矛盾 → 分配篇幅 → 定制注意事项 | System 输出格式前 | 推理引导 | P0 注意事项模板化 + P1 行业对比弱 |
| ④ | 输出格式增加「深度解读模式」（1 只 ETF）和「对比模式」（2-5 只）双分支 | System 输出格式 | 要求 | P1 单只 ETF 无规范 |
| ⑤ | 注意事项模板拆分：保留「历史业绩不代表未来表现」作为固定免责，其余改为动态生成（禁止通用模板） | System 输出格式 | 要求 | P0 注意事项模板化 |
| ⑥ | 日期上下文从 System 迁移到 User Prompt 顶部 + 措辞「生成 ETF 对比分析报告」→「生成报告」兼容单只场景 | User Prompt | 细节 | P2 日期动态化预备 |

### 修改前后对比

以下对比全部基于 Dify 线上真实输出（v2 / v3 各跑一次，同一输入）。

### C1：510300 vs 510310（同行业：两只沪深300 ETF）

#### 对比维度 1：核心矛盾识别（决策有用性①——对比维度有主次）

**v2 输出（维度对比总结）：**
> **1. 跟踪误差（精准度差异）** … **2. 基金规模与流动性** … **3. 近1年收益与近6月收益** …

（→ 三个维度平铺，无总起句，用户看完不知道"这两个差异哪个才是关键"）

**v3 输出（维度对比总结）：**
> 本轮两只 ETF 跟踪同一指数、费率完全相同，核心差异集中在以下两点：
> **1. 规模与流动性：华泰柏瑞显著占优** … **2. 跟踪精度：易方达略优但差距极小** …
> 收益方面：…不构成趋势性结论。

（→ CoT 第 1 步生效：先识别"同指数 + 同费率 → 核心矛盾是规模与精度"，再围绕核心矛盾组织，收益降级为补充说明）

#### 对比维度 2：注意事项动态化（P0 修复验证）

**v2 输出（注意事项）：**
> - 历史业绩不代表未来表现：…
> - 费率几乎相同：…
> - 跟踪误差需关注：…
> - **核心维度提示：跟踪误差、费率、规模是选 ETF 最常看的三个维度**。本案中费率完全相同，主要差异体现在规模与流动性上。

（→ 第 4 条前半句"跟踪误差、费率、规模是选 ETF 最常看的三个维度"仍是模板化残留）

**v3 输出（注意事项）：**
> - 历史业绩不代表未来表现：近1年收益差异可能在未来反转，不能作为选择依据
> - **流动性差异需结合自身资金量评估**：若单笔交易金额较大，华泰柏瑞今日成交额明显更高…；若为小额定投或长期持有，两者流动性均无实质问题
> - **费率完全相同，成本维度无差异**：两只 ETF 综合费率均为 0.2%，持有 5 年的成本差别为零，不必在费率上纠结
> - **跟踪精度差异极小**：0.0314% vs 0.0283% 在统计上微乎其微…

（→ CoT 第 3 步生效：4 条全部针对本次对比结果生成，模板化残留消失，且每条都给出"这意味着什么"）

#### 对比维度 3：场景锚定（决策有用性②——结论与用户决策挂钩）

**v2 输出：**
> 规模更大、成交额更高的 ETF，通常买卖价差更小，大额交易冲击成本更低，流动性体验更好。

（→ 通用知识陈述，没落回用户身上）

**v3 输出：**
> 流动性差异需结合自身资金量评估：若单笔交易金额较大…；若为小额定投或长期持有，两者流动性均无实质问题。

（→ 任务描述重写生效：把差异翻译成"取决于你的资金量"这一决策变量，用户可直接对号入座）

#### 对比维度 4：术语解释的呈现方式

**v2 输出：** 术语解释散落在逐只解读正文里（"跟踪误差反映 ETF 复制指数的精准度"），正文被解释打断；且对跟踪误差数值展开强行解释（"3.14% 高于常规被动 ETF 通常小于 0.5% 的水平，但该数值可能为特定统计口径下的年化值"）。

**v3 输出：** 术语注脚统一放在表格下方（"注：跟踪误差反映 ETF 复制指数的精准度，越小越准。综合费率 = 管理费 + 托管费…"），正文更聚焦；未对不确定的数据做强行解释。

（→ 「术语指南」独立成段后，模型把解释集中到注脚，正文可读性提升）

#### 对比维度 5：准确性——新暴露的数据层问题（非 prompt 问题）

两份输出对同一个数字 **跟踪误差 0.0314** 的单位解读不一致：

| 版本 | 解读 | 结论 |
|------|------|------|
| v2 | 0.0314 → **3.14%** | "高于被动 ETF <0.5% 基准线"→ 自我怀疑"可能为年化口径" |
| v3 | 0.0314 → **0.0314%** | "复制精度很高" |

根因：聚合 Code 节点输出的是裸数字 `0.0314`，没有带单位，模型只能凭常识猜测单位。这印证了成功标准「准确性第一」的优先级——**数据层不给单位，prompt 再怎么写也救不了**。该问题记入下一步迭代（聚合节点给跟踪误差加 `%` 单位）。

### C2：159995 vs 512690（跨行业：芯片 vs 酒）——P1 专项验证

#### 对比维度 1：跨行业差异归因（P1 核心修复验证）

**v2 输出（注意事项）：**
> 行业差异显著：芯片ETF聚焦科技/TMT行业（受技术进步、政策周期影响大），酒ETF行业分类为综合/其他（与消费场景、行业周期相关），两者所处赛道逻辑不同，波动特征天然存在差异。

（→ 行业差异只在注意事项里一句话带过，且未回答"收益差距这么大的原因到底是什么"）

**v3 输出（维度对比总结）：**
> 两者近 3 个月差距超过 45 个百分点，这反映的是底层资产（芯片 vs 白酒）的行业景气度和市场情绪差异，**而非基金运作水平的差距**。

（→ 跨行业对比最关键的一句话出现了：先做"归因分离"——把巨大收益差归因于行业景气度，而不是基金运作。这正是跨行业对比最容易出错的地方：模型容易把"半导体跌得多"误说成"华夏基金管理得差"）

#### 对比维度 2：核心矛盾识别

**v2 输出：** 基金规模、各期收益、今日成交额三个维度平铺，未区分主次。

**v3 输出：**
> 本轮对比核心矛盾：收益波动特征的极端分化。两者费率、成立年限接近，但近 3 个月呈现完全不同的走势节奏。

（→ 收益分化列为核心矛盾，费率/规模降级为"维度三：高度同质化"，主次分明）

#### 对比维度 3：场景锚定（决策有用性②）

**v3 独有（v2 无此写法）：**
> 如果投资者在 3 个月前买入 10 万元，现在账面大约只剩 5 万元。

（→ 把 -50.21% 翻译成可感知的绝对损失；v2 只说"近3月跌幅高达50.21%"）

#### 对比维度 4：不确定性的坦诚表达（决策有用性③）

**v2 输出：**
> 跟踪误差数据缺失，无法横向比较。

（→ 浅层陈述，就此打住）

**v3 输出：**
> 酒ETF的跟踪指数同样为"暂无数据"，报告无法确认其具体对标指数…投资者需自行核实其底层持仓逻辑。

（→ 不只说"数据缺失"，还给出"需自行核实"的行动指引。行业分类标注"综合/其他"时模型没有硬猜行业，跨行业对比中"无法确认对标指数"是重要提示）

#### 对比维度 5：注意事项模板化复验（P0 二次确认）

**v2 仍残留模板句：**
> 核心维度提示：综合费率（0.6%）、基金规模（芯片371.99亿 / 酒135.10亿）、跟踪误差均为筛选ETF时需重点关注的维度。

**v3 输出：** 5 条注意事项全部针对本次对比（极端回撤 / 跟踪误差缺失 / 对标指数缺失 / 双负收益 / 成立年限充分），模板句消失。

#### 对比维度 6：新发现——输出结构漂移（一致性风险）

v3 在规定的四段式之外，新增了独立「收益表现对比」章节，且对比总览表格中**丢失了「今日涨跌幅」行**（v2 有）。说明 CoT 强化推理后，模型开始"自由发挥"章节结构。本次漂移是"加内容"，未破坏可读性，但需在 C4（5 只）中重点观察结构稳定性。

另：v3 顶部出现"当前日期基准：2026 年 8 月，以下数据均在此时间框架内解读"开场白——日期上下文从 User Prompt 注入后，模型开始显式声明时间基准（v2 无此句），属预期中的正面副作用。

### C3：510300（单只 ETF）——P1 专项验证（深度解读模式）

#### 对比维度 1：单只场景行为切换（P1 核心修复验证）

**v2 输出：**
> 说明：本次请求仅提供单只 ETF 数据，无法进行多只 ETF 横向对比…

（→ 仍输出单列对比表格 + 四段式结构，其中「维度对比总结」变成空壳章节："由于本次数据仅包含一只ETF，无法进行多只产品间的差异对比"。v2 能"察觉"只有一只，但行为是降级为单列，仍套对比框架）

**v3 输出：**
> 深度解读报告：沪深300ETF华泰柏瑞（510300）
> 基本画像 → 指标解读 → 潜在关注点 → 注意事项

（→ 完全切换为 System Prompt 定义的「深度解读模式」，四段式对比框架消失，无空壳章节）

#### 对比维度 2：坦诚"无法回答选择问题"（决策有用性③）

**v3 独有（注意事项）：**
> 本报告仅基于单只ETF数据生成，未涉及同类产品对比，无法回答"是否应该选这只"的问题。

（→ 与任务描述"如果数据不足以支撑某个结论，坦诚说明，不强行给结论"完全一致；v2 对应写法是"未构成任何多产品对比结论"，v3 更直白地承认无法回答选择问题）

#### 对比维度 3：术语白话解释的组织

**v2：** 跟踪误差/费率/规模解释散落在维度总结中，无「白话解释」标记，成立日期未解释。

**v3：** 每个指标独立小节都带「白话解释」，成立日期在基本画像中解释（"运作超过14年，历史数据积累充足"）。

#### 对比维度 4：注意事项模板化复验（P0 三连确认）

**v2 仍残留：** "费率、规模、跟踪误差是选择ETF时需要重点关注的三个维度"

**v3：** 3 条注意事项全部针对本报告内容（近3月回调 / 历史业绩 / 无对比余地），模板句消失。

#### 对比维度 5：跟踪误差单位问题第三次出现

v3 输出 "跟踪误差：0.0314"，仍未带单位（C1 中 v2 解读为 3.14%、v3 解读为 0.0314%，本次 v3 直接输出裸数字）。同一问题三次出现，确认是聚合节点的数据格式缺陷，v4 必须修复。

### C4：510300,510310,159915,512880,588000（5 只边界）——鲁棒性验证

#### 对比维度 1：多只对比不崩（C4 核心考察）

5 只输入下 v2 / v3 均正常输出完整报告，未出现结构崩溃或超出 5 只上限的问题。v3 表格按需选列（纳入近1月/近3月收益），并在表格下方新增术语注脚段，统一解释成立日期/综合费率/跟踪误差。

#### 对比维度 2：跨行业归因分离（P1 多只场景复现）

**v2 输出：**
> 近1年收益从-10.78%到+62.41%的巨大分化，正是这种方向差异的体现。

（→ 有归因，但未明说"与基金能力无关"）

**v3 输出：**
> 这种分化并非基金公司能力差异，而是底层资产的行业属性差异所致。

（→ 明确的归因分离，跨行业多只场景同样生效）

#### 对比维度 3：维度"消解"——主次判断的高级形态（决策有用性①）

**v3 独有（跟踪质量维度）：**
> 有数据的 4 只跟踪误差在 0.0238%~0.0328% 之间，远优于 0.5% 的常见合格线…**选哪只差异都不大，核心差异仍在上层资产选择**。

（→ 不满足于"列出差异最大的维度"，而是主动消解一个伪差异维度——告诉用户"跟踪误差不用纠结，真正的决策变量是买哪个赛道"。这是 5 只高信息量输入下最有价值的收敛结论）

**v2 对应：** 仅罗列 4 只跟踪误差数值区间，未给出"不用纠结"的收敛判断。

#### 对比维度 4：注意事项模板化复验（P0 四连确认）

**v2 仍残留（第 4 次出现同一模板句）：** "综合费率、基金规模、跟踪误差是评估ETF的核心维度"

**v3：** 5 条注意事项全针对性，其中 3 条为 v2 没有的增量——费率劣势量化（"长期持有成本比其他 4 只高出 2 倍"）、风险承受能力提示（"需确认自身风险承受能力是否匹配"）、双胞胎 ETF 提示（"510300 与 510310 差异极小"）。

#### 对比维度 5：新观察——边界 case 下出现轻微建议性措辞

v3 注意事项第 4 条出现 "510300 明显更优"。虽上下文是客观分析（针对流动性），但"更优"已靠近比较性结论，与「禁止投资建议」准则存在张力。5 只高信息量输入下模型措辞更难收住，v4 需观察是否需要强化"对比性结论"的边界约束。

#### 剩余待测 case

- C5: 999999（无效代码）→ 考察错误兜底

### 代价/副作用

- **跟踪误差单位歧义（C1/C2/C3 三次出现，升级为 v4 必修复）**：聚合节点输出裸数字 `0.0314`，C1 中 v2 解读为 3.14%、v3 解读为 0.0314%（结论相反），C3 中 v3 直接裸数字输出。属数据层问题，v4 应让聚合节点输出时附带单位（如 `3.14%`）
- **输出结构漂移（C2 新发现）**：v3 在 C2 中新增了独立「收益表现对比」章节，且对比总览表格丢失「今日涨跌幅」行。CoT 强化推理后模型开始自主增删章节，本次是"加内容"无碍，若后续漂移到"缺章节"则需在 System Prompt 输出格式处加"章节不可增删"的硬约束
- **建议性措辞冒头（C4 新观察）**：5 只高信息量输入下，v3 出现"510300 明显更优"的比较性措辞，与「禁止投资建议」准则存在张力。当前可控，但说明信息量越大、模型越容易滑向结论性表达，v4 可考虑在行为准则中明确"比较结论仅限维度层面，不给产品层面的优劣结论"
- CoT 的三步推理是隐式的，DeepSeek V4 Flash 是否稳定执行需多 case 验证。如果模型跳过推理直接套模板，后续可能需要改为显式 CoT（要求先输出分析再输出报告，成本翻倍）
- 「深度解读模式」已通过 C3 验证：模型能正确识别单只输入并切换为深度解读模式（基本画像/指标解读/潜在关注点），隐式触发（`<etf_raw_data>` 仅 1 条）工作正常
- System Prompt 篇幅从 ~60 行增加到 ~75 行，但 CoT 部分是「内部思考」，不会增加用户可见的输出 Token

### 完整 Prompt 文本

**System Prompt（v3 完整版）：**

```
你是一位 ETF 研究员，擅长把复杂数据翻译成普通投资者能看懂、能用来做决策的白话解读。你的任务是：帮用户看清几只 ETF 之间真正重要的差异是什么，让用户能结合自身投资偏好做出判断。如果数据不足以支撑某个结论，坦诚说明，不强行给结论。

## 行为准则

1. 禁止提供投资建议。不说"建议买入"、"推荐持有"、"这只更好"等结论性判断
2. 禁止编造数据。只基于提供的真实数据，缺失数据标注"暂无数据"
3. 底部必须标注："数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考"
4. 结构化输出，格式见下方
5. 最多对比 5 只 ETF

## 术语指南

报告中首次出现以下术语时，必须用一句白话解释：
- 跟踪误差：反映 ETF 复制指数的精准度，越小越准。被动 ETF 通常 < 0.5%，若 > 3% 需特别说明风险
- 综合费率：管理费 + 托管费（两者每年从净值中扣除），是持有 ETF 的核心成本。两只 ETF 费率相差 0.1% 以上时，应计算 5 年累计成本差异
- 管理费率、托管费率、成立日期出现时也需简短白话解释

## 分析流程（内部思考，不输出给用户）

生成报告前，先在心里完成以下分析：
1. 识别核心矛盾：这几只 ETF 之间"最值得关注的分化点"是什么？
   - 可能是费率差距、规模差距、行业/指数的本质差异、或业绩分化
2. 根据核心矛盾，决定本次报告的侧重点和篇幅分配
3. 确定注意事项中需要特别提示的风险点（必须针对本次对比结果，禁止使用通用模板）

## 输出格式

**输入只有 1 只 ETF 时（深度解读模式）：**
生成「深度解读报告」，包含：
- 该 ETF 的基本画像（跟踪什么指数、什么行业、哪家基金公司）
- 各项指标意味着什么（费率水平、规模大小、跟踪误差、近期业绩表现分别说明）
- 该 ETF 的潜在关注点（如规模偏小 → 流动性风险、成立时间短 → 历史数据有限等，必须基于数据）

**输入 2-5 只 ETF 时（对比模式）：**

### 对比总览
用 Markdown 表格列出对比维度

### 逐只解读
每只 ETF 一段话（不超过 5 行），突出该 ETF 在本轮对比中最显著的特点

### 维度对比总结
围绕核心矛盾，选择差异最大的 2-3 个维度深入对比

### 注意事项
- 历史业绩不代表未来表现
- 根据本次对比结果，列出真正需要注意的风险点（禁止使用通用模板内容）

---
数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考。不构成投资建议。
```

**User Prompt（v3 完整版，Jinja2 模板）：**

```jinja
当前实际日期为 2026 年 08 月。请以此为基准判断数据中的日期是否正常，不要将 2026 年的日期误判为"未来日期"。

{% if has_data == "false" %}
当前数据服务不可用。请礼貌告知用户：暂时无法获取 ETF 对比数据（可能是数据源维护中），稍后重试。严禁编造任何数据。
错误详情（不向用户展示）：{{ error_message }}
{% else %}
{% if warning %}
> ⚠️ 重要提醒：{{ warning }}
>
> 请在报告的「注意事项」章节开头单独标注此提醒。
{% endif %}

请严格基于下方数据生成报告。全程遵守 SYSTEM 中的行为准则和输出格式要求。
严禁脱离给定数据编造行情、收益、风险等未给出的信息。

<etf_raw_data>
{{ comparison_data }}
</etf_raw_data>
{% endif %}
```

---

## v2 — 2026-08-11

### 触发原因

三个问题驱动本次迭代：
1. 用新成立 ETF（159017，成立日期 2026-04-22）测试时，LLM 将 2026-04-22 误判为"疑似未来日期"——因为模型训练数据截止 2025 年
2. 报告中基金规模显示 `+1.3400`、最新价显示 `+0.9130`——数字前有 `+` 号且无单位，LLM 无法正确解读
3. User Prompt 的错误路径和警告路径缺少明确行为指令——数据不可用时模型不知道应该输出什么，警告出现时不知道如何处理

### 修改了什么

**改动 1：System Prompt 新增日期上下文**

```
当前实际日期为 2026 年 08 月（你处在 2026 年，不是 2025 年）。
请以此为基准判断数据中的日期是否正常，不要将 2026 年的日期误判为"未来日期"。
```

**改动 2：User Prompt 重构（Jinja2 模板）**

| 路径 | v1 | v2 |
|------|----|----|
| 错误路径 | `当前数据服务不可用，错误信息：{{error}}` | 加了 `请直接礼貌告知用户暂时无法获取…严禁编造数据强行生成分析报告` |
| 警告路径 | `> ⚠️ 输入提示：{{warning}}` | 改为"重要提醒"，加了 `请在报告的「注意事项」章节开头单独标注此提醒` |
| 正常路径 | `请基于以下数据生成…` | 加了 `严禁脱离给定数据编造行情、收益、风险等未给出的信息`，数据用 `<etf_raw_data>` XML 标签包裹 |
| 变量引用 | 旧式 `{{#id.var#}}` | 改用 Jinja2 `{{ variable }}`（开启 Jinja 开关后变量语法自动升级） |

**改动 3：聚合 Code 节点数值格式化逻辑重写**

区分两类 float 的显示语义：
- **涨跌/收益率**（今日涨跌幅、近N周/月/年收益）→ 带 `+`/`-` 号，如 `+0.0089`
- **绝对数值**（基金规模、最新价、今日成交额）→ 不加符号，且带单位，如 `1.34 亿元`、`0.9130 元`

### 修改前后对比

**对比 1：日期误判（v1 vs v2 LLM 输出）**

v1 输出：
> 成立日期显示为 2026-04-22（疑似未来日期）

v2 输出：
> 成立于 2026-04-22，是一只较新的 ETF，历史数据较短

**对比 2：数值格式（传给 LLM 的数据矩阵）**

v1：
```
- 基金规模: +1.3400
- 最新价: +0.9130
```

v2：
```
- 基金规模: 1.34 亿元
- 最新价: 0.9130 元
```

### 代价/副作用

- 日期声明是静态文本，到 2027 年需要手动更新（或在后续版本改为动态注入）
- User Prompt 重构后变量语法从旧式 `{{#id.var#}}` 变为 Jinja2 `{{ var }}`，需要 Dify 节点开启 Jinja2 开关才能生效
- 成交额自动判断单位（>=1 亿显示"亿元"，否则显示"元"）

### 完整 Prompt 文本

**System Prompt（v2 完整版）：**

```
你是一个专业的 ETF 对比分析助手。你的任务是基于真实数据，帮用户对比多只 ETF 的核心维度差异。

当前实际日期为 2026 年 08 月（你处在 2026 年，不是 2025 年）。请以此为基准判断数据中的日期是否正常，不要将 2026 年的日期误判为"未来日期"。

## 行为准则

1. 禁止提供投资建议。不说"建议买入"、"推荐持有"、"这只更好"等结论性判断
2. 禁止编造数据。只基于提供的真实数据，缺失数据标注"暂无数据"
3. 必须用白话解释以下关键术语：
- 跟踪误差：反映 ETF 复制指数的精准度，越小越准。被动 ETF 通常 < 0.5%，若 > 3% 需特别说明风险
- 综合费率：管理费 + 托管费（两者每年从净值中扣除），是持有 ETF 的核心成本。两只 ETF 费率相差 0.1% 以上时，应计算 5 年累计成本差异
- 其他如"管理费率""托管费率""成立日期"也需简短白话解释
4. 底部必须标注："数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考"
5. 结构化输出，格式见下方
6. 最多对比 5 只 ETF

## 输出格式

### 对比总览
用 Markdown 表格列出对比维度

### 逐只解读
每只 ETF 一段话（不超过 5 行）

### 维度对比总结
选择差异最大的 2-3 个维度深入对比

### 注意事项
- 历史业绩不代表未来表现
- 费率、规模、跟踪误差是选 ETF 的核心维度

---
数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考。不构成投资建议。
```

**User Prompt（v2 完整版，Jinja2 模板）：**

```jinja
{% if has_data == "false" %}
当前数据服务不可用。请礼貌告知用户：暂时无法获取 ETF 对比数据（可能是数据源维护中），稍后重试。严禁编造任何数据。
错误详情（不向用户展示）：{{ error_message }}
{% else %}
{% if warning %}
> ⚠️ 重要提醒：{{ warning }}
>
> 请在报告的「注意事项」章节开头单独标注此提醒。
{% endif %}

请严格基于下方数据生成 ETF 对比分析报告。全程遵守 SYSTEM 中的行为准则和输出格式要求。
严禁脱离给定数据编造行情、收益、风险等未给出的信息。

<etf_raw_data>
{{ comparison_data }}
</etf_raw_data>
{% endif %}
```

---

## v1 — 2026-08-09{% if has_data == "false" %}
当前数据服务不可用。请礼貌告知用户：暂时无法获取 ETF 对比数据（可能是数据源维护中），稍后重试。严禁编造任何数据。
错误详情（不向用户展示）：{{ error_message }}
{% else %}
{% if warning %}
> ⚠️ 重要提醒：{{ warning }}
>
> 请在报告的「注意事项」章节开头单独标注此提醒。
{% endif %}

请严格基于下方数据生成 ETF 对比分析报告。全程遵守 SYSTEM 中的行为准则和输出格式要求。
严禁脱离给定数据编造行情、收益、风险等未给出的信息。

<etf_raw_data>
{{ comparison_data }}
</etf_raw_data>
{% endif %}（基线）

当前 Prompt 完整文本见 `dify/workflow-dsl.yml` 中「生成对比报告」LLM 节点的 `system` / `user` prompt。

### 版本特征摘要

| 维度 | 当前方案 |
|------|---------|
| 类型 | Zero-shot（无 Few-shot 示例）+ 轻度 CoT（要求"选择差异最大的维度深入对比"）+ Structured Output（四段式 Markdown） |
| 行为约束 | 6 条准则：禁投资建议 / 禁编造数据 / 白话解释术语 / 标注数据源 / 结构化输出 / 最多 5 只 |
| 输出格式 | 对比总览表格 → 逐只解读 → 维度对比总结 → 注意事项 |
| 术语解释 | 强制规则：跟踪误差、综合费率、管理费率、托管费率出现时必须跟白话解释 |
| 模型 | DeepSeek V4 Flash，temperature=0.3 |

### 完整 Prompt 文本

**System Prompt：**

```
你是一个专业的 ETF 对比分析助手。你的任务是基于真实数据，帮用户对比多只 ETF 的核心维度差异。

## 行为准则

1. 禁止提供投资建议。不说"建议买入"、"推荐持有"、"这只更好"等结论性判断

2. 禁止编造数据。只基于提供的真实数据，缺失数据标注"暂无数据"

3. 必须用白话解释以下关键术语：
- 跟踪误差：反映 ETF 复制指数的精准度，越小越准。被动 ETF 通常 < 0.5%，若 > 3% 需特别说明风险
- 综合费率：管理费 + 托管费（两者每年从净值中扣除），是持有 ETF 的核心成本。两只 ETF 费率相差 0.1% 以上时，应计算 5 年累计成本差异
- 其他如"管理费率""托管费率""成立日期"也需简短白话解释

4. 底部必须标注："数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考"

5. 结构化输出，格式见下方

6. 最多对比 5 只 ETF

## 输出格式

### 对比总览

用 Markdown 表格列出对比维度

### 逐只解读

每只 ETF 一段话（不超过 5 行）

### 维度对比总结

选择差异最大的 2-3 个维度深入对比

### 注意事项

- 历史业绩不代表未来表现
- 费率、规模、跟踪误差是选 ETF 的核心维度

---

数据来源：新浪财经 / 东方财富公开数据，延迟约 15-30 分钟，仅供参考。不构成投资建议。
```

**User Prompt（Dify 模板，含条件分支）：**

```
{% if has_data == "false" %}
当前数据服务不可用，错误信息：{{error_message}}
{% else %}
{% if warning %}
> ⚠️ 输入提示：{{warning}}
{% endif %}

请基于以下数据生成 ETF 对比分析报告，严格遵循 SYSTEM 中的行为准则和输出格式要求。

{{comparison_data}}
{% endif %}
```

### 已知待修复问题

- [ ] **行业 ETF 的跨行业差异解读偏弱**。对半导体 vs 白酒这类跨行业对比，模型仍需更明确的"行业差异"维度优先级提示
- [ ] **输出风格偶尔跑偏**。Zero-shot 下模型在不同输入间偶尔切换到学术论文风或过于口语化，需要 1-2 份标杆报告锁风格
- [ ] **注意事项模板化**。当前"注意事项"对所有对比都一样（"历史业绩不代表未来表现 / 费率规模跟踪误差是核心维度"），应根据具体对比结果动态调整
- [ ] **单只 ETF 输入时的行为未明确规范**。行为准则禁止投资建议，但未专门说明"只有 1 只 ETF 时应该输出什么"

---

## 版本历史

| 版本 | 日期 | 核心改动 | 解决的问题 |
|:---:|------|---------|----------|
| v2 | 08-09 | System Prompt 加日期上下文 + 聚合代码区分绝对值/涨跌幅格式化并添加单位 | 2026 年日期误判 + 数值缺单位 |
| v1 | 08-06 | 基线版本 | — |
